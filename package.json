{
  "name": "react-use-gestures",
  "version": "0.1.2",
  "description": "An utility function to use with React hooks that plays well with https://github.com/drcmda/react-spring 's useSpring()",
  "main": "dist/index.js",
  "scripts": {
    "start": "npm run build:only -- --watch --source-maps inline",
    "build": "npm run clean && NODE_ENV=production npm run build:only",
    "build:only": "babel index.mjs -d dist",
    "clean": "rm -rf dist",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/dotlouis/react-use-gestures.git"
  },
  "keywords": [
    "react",
    "hooks",
    "spring",
    "animation",
    "gestures",
    "touch"
  ],
  "author": "1891109+dotlouis@users.noreply.github.com",
  "license": "MIT",
  "bugs": {
    "url": "https://github.com/dotlouis/react-use-gestures/issues"
  },
  "homepage": "https://github.com/dotlouis/react-use-gestures#readme",
  "dependencies": {
    "@babel/cli": "^7.1.5",
    "@babel/core": "^7.1.5",
    "@babel/preset-env": "^7.1.5",
    "babel-preset-minify": "^0.5.0"
  }
}
